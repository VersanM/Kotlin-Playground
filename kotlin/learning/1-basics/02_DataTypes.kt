/**
 * Data Types
 *
 * Kotlin is a statically-typed language, meaning you can (but don’t have to) specify the data type. The common types are:
 *
 * Int (integer)
 * Double (floating point)
 * String (sequence of characters)
 * Boolean (true or false)
 * Char (single character)
 *
 */

fun main() {
    val integer: Int = 10
    val double: Double = 10.0
    val string: String = "Hello!"
    val boolean: Boolean = true
    val char: Char = 'A'

    println(integer)
    println(double)
    println(string)
    println(boolean)
    println(char)
}